{
  "name": "route-map-navigator-pro",
  "version": "2.0.0",
  "private": true,
  "description": "Static route planner and step-by-step map navigator.",
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "check": "node scripts/check-js.mjs",
    "verify": "npm run check && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
